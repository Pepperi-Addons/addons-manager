{
  "name": "typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:client && npm run build:server",
    "init": "npm install && cd ./server-side && npm install && cd .. && cd ./client-side && npm install && cd ..",
    "client:addon": "cd client-side && ng serve --port 4400 --configuration SingleSpa && cd..",
    "client:standalone": "cd client-side && ng serve --port 4400 --configuration Standalone && cd..",
    "build:client": "cd client-side && ng build --configuration SingleSpa && cd..",
    "build:server": "cd ./server-side && npm run build && cd ..",
    "publish-addon": "publish-addon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pepperi-addons/scripts": "^1.0.27"
  }
}
